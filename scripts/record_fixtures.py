"""Record DataHub responses to fixtures for the offline `make demo` path.

Runs the pipeline against a live DataHub and captures the read responses so the
fixtures can drive both the 60-second offline demo and the test suite
(architectural rule 3). Read docs/API-NOTES.md first.

STUB — populated in Phase 2.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


if __name__ == "__main__":
    raise SystemExit("record_fixtures.py is a stub — implemented in Phase 2.")
