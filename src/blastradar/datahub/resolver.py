"""Resolve changed columns to DataHub schemaField URNs.

Maps a (model, column) pair from the SQL delta analyzer onto the corresponding
dataset and column-level URN in DataHub. Read docs/API-NOTES.md first.

STUB — populated in Phase 1B.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)
