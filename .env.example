# Copy to `.env` (gitignored) and fill in. The Phase 0 scripts read these.
# Get a token from the DataHub UI: Settings -> Access Tokens -> Generate.

DATAHUB_GMS_URL=http://localhost:8080
DATAHUB_GMS_TOKEN=

# Phase 1C (narration) will also need, later:
# ANTHROPIC_API_KEY=
